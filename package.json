{
  "name": "trello-mcp-server",
  "version": "1.0.0",
  "description": "Cloudflare Worker MCP server for Trello",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "dev": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250609.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.114.0"
  }
}
